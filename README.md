# Garage_Door
A repo for the contents of my Raspberry Pi Garage door checker
## Setup
In folder, run: 
```
python3 -m venv env
source env/bin/activate
python -m pip install --upgrade pip
pip install requirements.txt
```
